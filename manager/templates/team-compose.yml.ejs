# Generated by a1engineer-manager — do not edit manually
# Full template delivered by #8 (baku-dev). This is a stub.
version: "3.9"

networks:
  net-<%= team.name %>:
    driver: bridge

services:
<%_ for (const agent of team.agents) { _%>
  agent-<%= agent.role %>:
    image: a1engineer/agent:latest
    networks:
      - net-<%= team.name %>
    environment:
      - IRC_ROLE=<%= agent.role %>
      - IRC_CITY=<%= team.name %>
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    restart: unless-stopped
<%_ } _%>
